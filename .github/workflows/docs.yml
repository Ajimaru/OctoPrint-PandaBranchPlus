name: docs

on:
  push:
    branches: [main]
    paths:
      - "docs/**"
      - "mkdocs.yml"
      - "requirements-docs.txt"
      - "scripts/generate-jsdocs.sh"
      - "octoprint_pandabranchplus/**/*.py"
      - "octoprint_pandabranchplus/static/js/**/*.js"
      - ".github/workflows/docs.yml"
  workflow_dispatch:

concurrency:
  group: pages
  cancel-in-progress: false

# Least-privilege at the workflow level; the deploy job widens to the Pages
# permissions it actually needs.
permissions:
  contents: read

jobs:
  build:
    name: Build site
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Set up Python 3.12
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip

      - name: Install docs dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-docs.txt
          # mkdocstrings needs to import the package to read docstrings; the
          # plugin itself does not declare OctoPrint as a dependency (it is
          # installed into an existing OctoPrint), so install it explicitly.
          pip install octoprint
          pip install -e .

      - name: Set up Node.js 20
        uses: actions/setup-node@v7
        with:
          node-version: "20"

      - name: Generate JavaScript API docs
        run: |
          npm install --no-save jsdoc-to-markdown
          ./scripts/generate-jsdocs.sh

      - name: Build MkDocs site (strict)
        run: mkdocs build --strict

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: ./site

  deploy:
    name: Deploy to GitHub Pages
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
